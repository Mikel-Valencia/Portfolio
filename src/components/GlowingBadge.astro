<div {...Astro.props} class="badge">
  <span class="badge__wrapper">
    <span class="badge__border"></span>
    <div class="badge__content">
      <slot />
    </div>
  </span>
</div>

<style lang="scss">
  // Dark-light mode variables.
  .badge {
    --text-glowing-badge: hsl(226, 91%, 46%, 1);
		--bg-glowing-badge: hsl(180, 92%, 95%, 1);
		--border-b1-glowing-badge: hsl(230, 99%, 49%, 1);
		--border-b2-glowing-badge: hsl(179, 96%, 50%, 1);
  }
  :global([data-theme="dark"]) .badge {
    --text-glowing-badge: hsl(293, 74%, 36%, 1);
		--bg-glowing-badge: hsl(268, 100%, 96%, 1);
		--border-b1-glowing-badge: hsl(293, 99%, 49%);
		--border-b2-glowing-badge: hsl(179, 96%, 50%, 1);
  }

  .badge {
    display: flex;
    align-items: center;
    &__wrapper {
      position: relative;
      overflow: hidden;
      display: inline-flex;
      border-radius: .3rem;
      padding: 1.5px;
    }
    &__border {
      position: absolute;
      inset: -1000%;
      background-image: conic-gradient(from 90deg at 50% 50%, var(--border-b2-glowing-badge), var(--border-b1-glowing-badge), var(--border-b2-glowing-badge));
      animation: spin 2s linear infinite;
    }
    &__content {
      display: inline-flex;
      justify-content: center;
      align-items: center;
      gap: .25rem;
      width: 100%;
      padding: .25rem .5rem;
      color: var(--text-glowing-badge);
      background-color: var(--bg-glowing-badge);
      border-radius: .3rem;
      font-size: var(--fs-glowing-badge, 1rem);
      line-height: var(--lh-glowing-badge, 1.5rem);
      font-weight: 500;
      backdrop-filter: blur(64px);
      white-space: nowrap;
    }
    @keyframes spin {
      from {
        transform: rotate(0deg);
      }
      to {
        transform: rotate(360deg);
      }
    }
  }
</style>