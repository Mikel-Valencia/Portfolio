<div {...Astro.props} class="badge">
  <slot />
</div>

<style lang="scss">
  .badge {
    --text-badge: var(--text-secondary);
    --bg-badge: var(--bg-secondary);
    --border-color-badge: var(--border-color);
    display: inline-flex;
    justify-content: center;
    align-items: center;
    gap: .25rem;
    max-width: fit-content;
    padding: .25rem .5rem;
    color: var(--text-badge);
    background-color: var(--bg-badge);
    border: var(--border-color-badge) solid 1px;
    border-radius: 9999px;
    font-size: 1rem;
    line-height: 1.5rem;
    @media (prefers-color-scheme: dark) {
      --text-badge: ;
      --bg-badge: ;
      --border-color-badge: ;
    }
  }
</style>